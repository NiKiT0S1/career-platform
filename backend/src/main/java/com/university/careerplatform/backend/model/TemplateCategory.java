/**
 * Enum representing template categories.
 * Used to distinguish between resume templates, contract templates, etc.
 */

package com.university.careerplatform.backend.model;

public enum TemplateCategory {
    CV,
    CONTRACT,
    OTHER
}
